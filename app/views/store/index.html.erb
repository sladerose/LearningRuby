<div class="w-full">
  <div class="relative isolate overflow-hidden bg-slate-900 py-24 sm:py-32 rounded-3xl mb-16 shadow-2xl">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
      <div class="mx-auto max-w-2xl lg:mx-0">
        <h2 class="text-5xl font-display font-bold tracking-tight text-white sm:text-7xl">Style defined.</h2>
        <p class="mt-6 text-lg leading-8 text-slate-300">Discover our curated collection of premium goods, designed for those who value quality and minimalist aesthetics.</p>
      </div>
    </div>
    <div class="absolute -z-10 bg-gradient-to-tr from-primary-500 to-indigo-900 opacity-20 inset-0"></div>
  </div>

  <div id="products" class="grid grid-cols-1 gap-x-8 gap-y-12 sm:grid-cols-2 lg:grid-cols-3 xl:gap-x-10">
    <% if @products.any? %>
      <% @products.each do |product| %>
        <div class="group relative flex flex-col bg-white rounded-2xl shadow-sm border border-slate-100 overflow-hidden transition-all duration-300 hover:shadow-xl hover:-translate-y-1">
          <div class="aspect-h-1 aspect-w-1 w-full overflow-hidden bg-slate-100 lg:aspect-none group-hover:opacity-90 transition-opacity">
            <% if product.image.attached? %>
              <%= image_tag product.image, class: "h-[320px] w-full object-cover object-center lg:h-[320px]" %>
            <% else %>
              <div class="h-[320px] w-full flex items-center justify-center bg-slate-50 text-slate-300">
                <svg class="h-12 w-12" fill="none" viewBox="0 0 24 24" stroke-width="1" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" d="m2.25 15.75 5.159-5.159a2.25 2.25 0 0 1 3.182 0l5.159 5.159m-1.5-1.5 1.409-1.409a2.25 2.25 0 0 1 3.182 0l2.909 2.909m-18 3.75h16.5a1.5 1.5 0 0 0 1.5-1.5V6a1.5 1.5 0 0 0-1.5-1.5H3.75A1.5 1.5 0 0 0 2.25 6v12a1.5 1.5 0 0 0 1.5 1.5Zm10.5-11.25h.008v.008h-.008V8.25Zm.375 0a.375 0 1 1-.75 0 .375 0 0 1 .75 0Z" />
                </svg>
              </div>
            <% end %>
          </div>

          <div class="flex flex-1 flex-col p-6">
            <div class="flex justify-between items-start mb-2">
              <h3 class="text-lg font-display font-semibold text-slate-900">
                <%= product.name %>
              </h3>
              <p class="text-lg font-display font-bold text-primary-600"><%= number_to_currency(product.price) %></p>
            </div>
            <p class="text-sm text-slate-500 line-clamp-2 mb-6 flex-grow"><%= product.description %></p>
            
            <div class="mt-auto flex items-center justify-between">
              <% if product.stock > 0 %>
                <%= button_to line_items_path(product_id: product.id), class: "w-full rounded-full bg-slate-900 px-6 py-3 text-sm font-semibold text-white shadow-sm hover:bg-primary-600 transition-all active:scale-95 flex items-center justify-center gap-2" do %>
                  <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
                  </svg>
                  Add to Cart
                <% end %>
              <% else %>
                <div class="w-full text-center rounded-full bg-slate-100 px-6 py-3 text-sm font-semibold text-slate-400 cursor-not-allowed">
                  Out of Stock
                </div>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    <% else %>
      <div class="col-span-full text-center py-12">
        <p class="text-gray-500 text-lg">No products available yet.</p>
      </div>
    <% end %>
  </div>
</div>
